name: Build Android APK

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/build-apk.yml'
  workflow_dispatch:

jobs:
  build-apk:
    name: Build TheDeep CleanerZ Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 21 JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept Licenses & Install Android SDK Platforms
        run: |
          yes | sdkmanager --licenses || true
          sdkmanager "platforms;android-35" "platforms;android-36" "build-tools;35.0.0"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend Dependencies
        working-directory: frontend
        run: |
          npm install
          node scripts/patch-capacitor.js

      - name: Build Mobile Web Application & Sync Assets
        working-directory: frontend
        run: npm run build:mobile

      - name: Patch Native Dependencies
        working-directory: frontend
        run: node scripts/patch-capacitor.js

      - name: Build Android Debug APK
        working-directory: frontend/android
        run: |
          chmod +x ./gradlew
          set -o pipefail
          ./gradlew assembleDebug --no-daemon --stacktrace --info 2>&1 | tee ../../gradle_build.log

      - name: Process Build Results & Annotations
        if: always()
        run: |
          node frontend/scripts/report-gradle-error.js

      - name: Locate Generated APK
        run: |
          ls -la frontend/android/app/build/outputs/apk/debug/ || true
          find frontend/android -name "*.apk"

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TheDeepCleanerZ-App-Debug
          path: frontend/android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14

      - name: Upload Gradle Diagnostics
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gradle-build-log
          path: gradle_build.log
          if-no-files-found: warn
          retention-days: 7
